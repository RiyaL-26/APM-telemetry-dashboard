# APM-telemetry-dashboard
Full-stack APM control panel streaming simulated system logs to an optimized SQLite datastore with automated threshold alert detection, responsive UI filtering, auto-polling metrics, and containerized deployment via Docker Compose.
